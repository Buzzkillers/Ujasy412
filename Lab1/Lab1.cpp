#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

using namespace std;
int iNameName = 0;
wchar_t m = L'^';

vector<vector<int>> arrNum =
	{ 7, 8, 90, 100 };

arrNum.push_back(98);
arrNum.push_back(98);
arrNum.pop_back();

int main() {
	double dNeedName = 3.1415;
	cout << "Hello World!\n"; //Ctrl+D Дублируем строку
	cout << dNeedName;
}
